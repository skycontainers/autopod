---
#
# Java Install Playbook
#


- hosts: all
  sudo: yes
  remote_user: tclouds
  tasks:
  - hostname: name='mesos-master-{{zoo_id}}'
  
  roles:
    - {role: ../roles/openjdk }
    - {role: ../roles/supervisord }
    - {role: ../roles/zookeeper }

